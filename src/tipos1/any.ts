let valorNumerico: any

valorNumerico = 12;
valorNumerico = '12';

function soma(a: number, b: number) {
    return a + b; 
}

// por padrao já vem como any. é recomendado não usar para não quebrar a build

